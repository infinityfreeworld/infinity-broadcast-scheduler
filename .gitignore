node_modules/
dist/
.env
.env.local
*.log

# Piper binaries + voices (téléchargés à chaque CI run, cachés via actions/cache)
piper/
voices/

# Outputs locaux
out/
*.wav
*.mp3

# Environnement Python de Kokoro (voix chinoises) — séparé de celui de Piper
.venv-kokoro/
